import React,{useState, useEffect} from 'react';

const MessageContainer = () => {

    useEffect(() => {
        
    }, []);

    return (
        <div>
            
        </div>
    );
}

export default MessageContainer;
